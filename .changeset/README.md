# Changesets

Release notes live here as `*.md` files created with `bunx changeset`.
`just release` runs `changeset version`, builds, and publishes. The three
`@earcon/*` packages are a `fixed` group and always share one version.
